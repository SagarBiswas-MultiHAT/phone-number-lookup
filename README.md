# phone-number-lookup
A Python-based phone number lookup app using Tkinter for GUI. It validates and retrieves information about a phone number, including carrier, country, and timezone. The app also fetches and displays the corresponding country flag. Features include clipboard copying and input clearing.
